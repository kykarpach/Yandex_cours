// Умный каталог (SmartCatalogue) 📋
// Ограничение времени: 1 с
// Ограничение памяти: 64.0 Мб

// Реализуйте шаблонный класс SmartCatalogue<T>, который выступает в роли хранилища элементов произвольного типа T. Класс должен обеспечивать безопасное чтение данных через указатели и уметь фильтровать содержимое с помощью стандартных алгоритмов.

// Требования к классу:

// Класс должен называться SmartCatalogue и иметь шаблонный параметр T.

// Должен присутствовать конструктор по умолчанию.

// Метод void Insert(const T& item); — добавляет копию элемента в каталог.

// Метод void Insert(const T* items, size_t count); — принимает сырой указатель на начало массива и количество элементов. Должен скопировать count элементов в каталог.

// Метод const T* FindFirst(bool (*predicate)(const T&)) const; — принимает указатель на функцию-предикат. Метод должен найти первый элемент, для которого предикат вернул true, и вернуть константный указатель на него в памяти каталога. Если элемент не найден, возвращается nullptr.

// Метод size_t RemoveLessThan(const T& threshold); — физически удаляет из каталога все элементы, которые строго меньше threshold (предполагается, что для типа T определен оператор <). Возвращает количество удаленных элементов.

// Примечание:
// Сдайте только код класса SmartCatalogue без функции main. В реализации методов FindFirst и RemoveLessThan настоятельно рекомендуется использовать алгоритмы из библиотеки <algorithm> (например, std::find_if и связку std::remove_if + erase).



template<typename T>
class SmartCatalogue{
private:



public:

SmartCatalogue();   


};